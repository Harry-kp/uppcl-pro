import type { Metadata } from "next";

export const metadata: Metadata = { title: "1912 Complaints" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
