# ─── Secrets / local-only ────────────────────────────────────────────────
.env
.env.local
.env.*.local
uppcl_session.json

# Real PII used by screenshot redaction + PII scan. Sample is committed.
scripts/pii.json

# One-off user-to-user handoff note (staged copies, personal instructions)
HANDOFF.md

# ─── Browser captures (likely contain JWTs / PII) ────────────────────────
*.har

# ─── Design references (kept out of git — see CLAUDE.md) ─────────────────
stitch_with_google_ai_design/

# ─── Python ──────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.egg-info/
venv/
.venv/
env/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# ─── Node / Next.js ──────────────────────────────────────────────────────
web/node_modules/
web/.next/
web/out/
web/*.tsbuildinfo
web/.vercel/

# ─── Build artefacts ─────────────────────────────────────────────────────
dist/
build/

# ─── Claude Code local files (runtime / user-specific) ──────────────────
.claude/settings.local.json
.claude/scheduled_tasks.lock

# ─── OS / editor junk ────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
